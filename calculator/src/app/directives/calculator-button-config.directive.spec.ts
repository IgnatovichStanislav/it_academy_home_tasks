import { CalculatorButtonConfigDirective } from './calculator-button-config.directive';

describe('CalculatorButtonConfigDirective', () => {
  it('should create an instance', () => {
    const directive = new CalculatorButtonConfigDirective();
    expect(directive).toBeTruthy();
  });
});
